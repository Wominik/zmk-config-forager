parse_config:
  # NOTE: this replaces the tool's built-in punctuation map wholesale, so the
  # defaults are repeated here verbatim before adding our own entries below.
  zmk_keycode_map:
    EXCLAMATION: "!"
    EXCL: "!"
    AT_SIGN: "@"
    AT: "@"
    HASH: "#"
    POUND: "#"
    DOLLAR: "$"
    DLLR: "$"
    PERCENT: "%"
    PRCNT: "%"
    CARET: "^"
    AMPERSAND: "&"
    AMPS: "&"
    ASTERISK: "*"
    ASTRK: "*"
    STAR: "*"
    LEFT_PARENTHESIS: "("
    LPAR: "("
    RIGHT_PARENTHESIS: ")"
    RPAR: ")"
    EQUAL: "="
    PLUS: "+"
    MINUS: "-"
    UNDERSCORE: "_"
    UNDER: "_"
    SLASH: "/"
    FSLH: "/"
    QUESTION: "?"
    QMARK: "?"
    BACKSLASH: "\\"
    BSLH: "\\"
    PIPE: "|"
    NON_US_BACKSLASH: "\\"
    PIPE2: "|"
    NON_US_BSLH: "|"
    SEMICOLON: ";"
    SEMI: ";"
    COLON: ":"
    SINGLE_QUOTE: "'"
    SQT: "'"
    APOSTROPHE: "'"
    APOS: "'"
    DOUBLE_QUOTES: '"'
    DQT: '"'
    COMMA: ","
    LESS_THAN: "<"
    LT: "<"
    PERIOD: "."
    DOT: "."
    GREATER_THAN: ">"
    GT: ">"
    LEFT_BRACKET: "["
    LBKT: "["
    LEFT_BRACE: "{"
    LBRC: "{"
    RIGHT_BRACKET: "]"
    RBKT: "]"
    RIGHT_BRACE: "}"
    RBRC: "}"
    GRAVE: "`"
    TILDE: "~"
    NON_US_HASH: "#"
    NUHS: "#"
    TILDE2: "~"

    # --- symbols added on top of the defaults above ---

    # modifiers
    LGUI: "⌘"
    RGUI: "⌘"
    GUI: "⌘"
    LALT: "⌥"
    RALT: "⌥"
    ALT: "⌥"
    LCTRL: "⌃"
    RCTRL: "⌃"
    CTRL: "⌃"
    LSHFT: "⇧"
    RSHFT: "⇧"
    SHIFT: "⇧"

    # arrows / navigation
    LEFT: "←"
    RIGHT: "→"
    UP: "↑"
    DOWN: "↓"
    HOME: "⇱"
    END: "⇲"
    PG_UP: "⇞"
    PG_DN: "⇟"

    # editing
    TAB: "⇥"
    SPACE: "␣"
    BSPC: "⌫"
    DEL: "⌦"
    RET: "⏎"
    ENTER: "⏎"
    ESC: "⎋"

  # symbols for modifiers wrapped in modifier functions, e.g. LC(LALT), LG(V)
  modifier_fn_map:
    left_ctrl: "⌃"
    right_ctrl: "⌃"
    left_shift: "⇧"
    right_shift: "⇧"
    left_alt: "⌥"
    right_alt: "⌥"
    left_gui: "⌘"
    right_gui: "⌘"
    keycode_combiner: "{mods}{key}"
    mod_combiner: "{mod_1}{mod_2}"
    special_combinations:
      left_ctrl+left_alt+left_gui+left_shift: "⌃⌥⌘⇧"
      left_ctrl+left_alt+left_shift: "⌃⌥⇧"

  # HPR_G / HPR_H (the Hyper hold on the G/H keys) are the same 4-mod chord
  # spelled out on every key already via modifier_fn_map above; showing that
  # full combo again under G and H is redundant, so collapse it to a single
  # glyph there and explain it once in the footer instead (see draw_config).
  raw_binding_map:
    "&hml LS(LA(LC(LGUI))) G": { t: G, h: "✦" }
    "&hmr RS(RA(RC(RGUI))) H": { t: H, h: "✦" }

draw_config:
  footer_text: "✦ = ⌃⌥⌘⇧"
  svg_extra_style: |
    text.footer { text-anchor: middle; }
